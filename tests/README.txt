Building
========

export BASEDIR=/usr/lib (or set this to a location where you have installed CCNx)

Info on how to run tests
========================

This relies on google test.

Add test cases to the Makefile and add your tests directly here.
